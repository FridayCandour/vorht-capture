package com.vorht.capture.net

/** All remote endpoints in one place. No localhost anywhere. */
object VorhtEndpoints {
    /** Ops relay: crash reports + captured WhatsApp details → team WhatsApp chat. */
    const val CARLA = "https://carla.codedynasty.dev"

    /** Project identifier used for crash/error reports. */
    const val PROJECT = "vorht-capture"
}
